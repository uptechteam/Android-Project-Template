<?xml version="1.0"?>
<template
    format="5"
    revision="1"
    name="UPTech starter project Activity"
    description="Creates a new activity, add common used libraries and generte boilerplate code for them"
    requireAppTheme="true"
    minApi="16"
    minBuildApi="16">

    <dependency name="android-support-v4" revision="8" />

    <category value="Activity" />
    <formfactor value="Mobile" />

    <!-- General fields -->
    <parameter
        id="activityClass"
        name="Activity Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MainActivity"
        help="The name of the activity class to create" />

    <parameter
        id="layoutName"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        suggest="${activityToLayout(activityClass)}"
        default="activity_login"
        help="The name of the layout to create for the activity" />

    <parameter
        id="activityTitle"
        name="Title"
        type="string"
        constraints="nonempty"
        default="Sign in"
        help="The name of the activity." />

    <parameter
        id="application_class"
        name="Class extends Application"
        type="string"
        constraints="class|unique|nonempty"
        default="MyApplication"
        help="The name of the application class" />

    <!-- Base Activity and Fragment -->
    <parameter
        id="genereteBaseActivity"
        name="Create BaseActivity class"
        type="boolean"
        default="true"
        help="If true, creates base Activity class" />

    <parameter
        id="baseActivityName"
        name="Base Activity Name"
        type="string"
        constraints="class|unique|nonempty"
        visibility="genereteBaseActivity"
        default="BaseActivity"
        help="The name of the base activity class" />

    <parameter
        id="genereteBaseFragment"
        name="Create Base Fragment class"
        type="boolean"
        default="true"
        help="If true, creates base Fragment class" />

    <parameter
        id="BaseFragmentName"
        name="Base Fragment Name"
        type="string"
        constraints="class|unique|nonempty"
        visibility="genereteBaseFragment"
        default="BaseFragment"
        help="The name of the base fragment class" />

    <!-- Build types fields -->
    <parameter
        id="addBuildTypes"
        name="Create different build types for app"
        type="boolean"
        default="true"
        help="If true, creates two build types for app" />

    <parameter
        id="releseBuildType"
        name="build type name"
        type="string"
        constraints="unique|nonempty"
        default="release"
        visibility="addBuildTypes"
        help="The name of the build type" />

    <parameter
        id="debugBuildType"
        name="build type name"
        type="string"
        constraints="unique|nonempty"
        default="debug"
        visibility="addBuildTypes"
        help="The name of the build type" />

    <!-- Libraties paraneters -->
    <parameter
        id="includeDagger"
        name="Include Dagger2 into the project"
        type="boolean"
        default="true"
        help="If true, include dagger and add basic configuretions" />

    <parameter
        id="generateClassesWithDefaultNames"
        name="Ganarete configuration classes with default names"
        type="boolean"
        default="true"
        visibility="includeDagger"
        help="Default names for configuration classes: AppModule, AppComponent, AppPreferences" />

    <parameter
        id="app_module"
        name="Module Name"
        type="string"
        constraints="class|unique|nonempty"
        default="AppModule"
        visibility="!generateClassesWithDefaultNames"
        help="The name of the activity class to create" />

    <parameter
        id="app_component"
        name="Component Name"
        type="string"
        constraints="class|unique|nonempty"
        default="AppComponent"
        visibility="!generateClassesWithDefaultNames"
        help="The name of the activity class to create" />

    <parameter
        id="app_preferences"
        name="Preferences class Name"
        type="string"
        constraints="class|unique|nonempty"
        default="AppPreferences"
        visibility="!generateClassesWithDefaultNames"
        help="The name of the activity class to create" />

    <parameter
        id="includeRetrofit"
        name="Include Retrofit into the project"
        type="boolean"
        default="true"
        help="If true, include retrofit" />

    <parameter
        id="generateApiClassesWithDefaultNames"
        name="Generate Api classes with default names"
        type="boolean"
        default="true"
        visibility="includeRetrofit"
        help="If true, genatates Api and ApiService classes" />

    <parameter
        id="apiClass"
        name="Api class name"
        type="string"
        constraints="class|unique|nonempty"
        default="Api"
        visibility="!generateApiClassesWithDefaultNames"
        help="The name of the Api interface" />

    <parameter
        id="apiService"
        name="ApiService class name"
        type="string"
        constraints="class|unique|nonempty"
        default="ApiService"
        visibility="!generateApiClassesWithDefaultNames"
        help="The name of the ApiService class" />

    <parameter
        id="includeButterknife"
        name="Include Butterknife into the project"
        type="boolean"
        default="true"
        help="If true, include dagger and add basic configuretions" />

    <parameter
        id="includeRx"
        name="Include Rx into the project"
        type="boolean"
        default="true"
        help="If true, include Rx" />

    <parameter
        id="includeStetho"
        name="Include Stetho into the project"
        type="boolean"
        default="true"
        help="If true, include Stetho" />

    <parameter
        id="includeGlide"
        name="Include Glide into the project"
        type="boolean"
        default="true"
        help="If true, include Glide" />

    <parameter
        id="addStaticChecks"
        name="Include Static Checks"
        type="boolean"
        default="true"
        help="If true, includes Static code checks : Checkstyle, PMD, Lint, Findbugs" />

    <parameter
        id="packageName"
        name="Package name"
        type="string"
        constraints="package"
        default="com.mycompany.myapp" />

    <thumbs>
        <thumb>template_uptech_activity.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
